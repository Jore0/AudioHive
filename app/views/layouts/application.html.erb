<!DOCTYPE html>
<html>
  <head>
    <title>AudioHive</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag   'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <script type="text/javascript">
        window.soundcloud = "<%= image_url("soundcloud") %>";
        window.create = "<%= image_url("create") %>";
        window.login = "<%= image_url("login") %>";
        window.header1 = "<%= image_url("header1") %>";
        window.header2 = "<%= image_url("header2") %>";

    </script>
    <%= yield %>
  </body>
</html>
